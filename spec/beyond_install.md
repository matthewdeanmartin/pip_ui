GUI for:
  - well known alternative repos, e.g. pypi test & piwheels
    - I want it to be as easy as picking from a drop down. It should be in the top. It should be config aware (i.e. if there are repos listed in config.)
  - custom/private package repos
    - I want a dialog box for adding a custom repo
GUI for corporate peeps:
  - GUI for helping with HTTPS certs. 
    - Here is a folder, try out all the certs, tell user what is good
    - Here is a cert, try it out immediately, tell user if it is usable
  - GUI for HTTP proxies
GUI for secrets
    - I don't know what it would look like, but we should have pervasive support for helping the user keep their credentials safe.
Pervasive Copy-As-Python Button 
  - like postman, but for pip commands, and it should give the user the subprocess code to run the pip command.
Update docs.